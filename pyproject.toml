[project]
name = "ankigen"
version = "0.1.0"
description = "Generate Anki Cards"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
